This C program implements a unix shell that can be used to run any command.
System call used are -fork, exec and many more
Has a feature of accesing history and running the last commands.

Run by
$ make
$ ./shell
