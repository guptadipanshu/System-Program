Implements a HTTP 1.1 server that has the feature of connection Keep Alive to save RTTs

To Run-
Running the web server:
%> make
%> ./server <port#> ...where <port#> is a port number. 
eg ./server 8888
(the port maynot free after use kindly change it next time) 
Running the client:
When you run the web server and the browser on the same machine, you can simply use: 
http://localhost:<port#>/
